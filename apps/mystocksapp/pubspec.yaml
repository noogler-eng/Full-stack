name: mystocksapp
description: "A new Flutter project."
version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  # move out from current repo to a separate package and 
  # import it here
  flutter_ui_kit:
    path: ../../packages/flutter_ui_kit
  api_client:
    path: ../../packages/api_client
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true